{
  "name": "walter-eletricista",
  "version": "1.0.0",
  "private": true,
  "description": "Landing page estática do Walter — Eletricista Profissional em Nova Friburgo e Região (RJ)",
  "scripts": {
    "clean": "node scripts/copy.js --clean",
    "copy": "node scripts/copy.js",
    "watch:css": "tailwindcss -i ./src/css/main.css -o ./dist/css/styles.css --watch",
    "serve": "live-server dist --port=3000 --no-browser",
    "dev": "npm run copy && concurrently -k -n css,copy,serve -c yellow,cyan,green \"npm:watch:css\" \"npm:copy -- --watch\" \"npm:serve\"",
    "images": "node scripts/images.js",
    "build:css": "tailwindcss -i ./src/css/main.css -o ./dist/css/styles.css --minify",
    "build": "npm run clean && npm run copy && npm run build:css"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.20",
    "concurrently": "^9.1.2",
    "live-server": "^1.2.2",
    "postcss": "^8.4.49",
    "sharp": "^0.35.3",
    "tailwindcss": "^3.4.17"
  }
}
